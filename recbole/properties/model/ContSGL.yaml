type: 'ED'                      # (str) The type to generate views. Range in ['ED', 'ND', 'RW'].
n_layers: 2                     # (int) The number of layers in SGL. 
ssl_tau: 0.2                     # (float) The temperature in softmax.
reg_weight: 1e-5                # (float) The L2 regularization weight.
ssl_weight: 0.05                # (float) The hyperparameter to control the strengths of SSL.
drop_ratio: 0.1                 # (float) The dropout ratio.
embedding_size: 128               # (int The embedding size of users and items.
llm_embedding_size: 768 
emb_selection: False
use_connector: True
connector_hidden_size: 256